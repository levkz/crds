version: "2"
sql:
  - engine: "sqlite"
    queries: "queries/"
    schema: "schema.sql"
    gen:
      go:
        package: "db"
        out: "db"
        emit_json_tags: false
        emit_db_tags: true
        emit_empty_slices: true
        emit_pointers_for_null_types: true
